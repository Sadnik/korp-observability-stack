# group_vars

This directory stores Ansible variables that apply to all hosts in the inventory.

- `all.yml` contains shared configuration values for the playbook.
- Ansible automatically loads this file when running the playbook.
