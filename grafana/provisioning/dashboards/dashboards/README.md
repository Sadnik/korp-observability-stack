# Grafana dashboards

This directory contains dashboard JSON files provisioned into Grafana automatically.

- `http-server-projeto-korp-dashboard.json` provides a dashboard for monitoring the Projeto Korp Go service.

Grafana reads these files from the provisioning configuration in `grafana/provisioning/dashboards/provider.yml`.
