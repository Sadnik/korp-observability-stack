# Grafana dashboards provisioning

This directory contains dashboard provisioning files for Grafana.

- `provider.yml` configures Grafana to load dashboard JSON files from the nested `dashboards/` directory.
- `dashboards/` contains the actual dashboard JSON definitions.
