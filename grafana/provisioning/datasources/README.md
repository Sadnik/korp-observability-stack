# Grafana datasources

This directory contains datasource definitions for Grafana.

- `prometheus.yml` defines the Prometheus datasource used by Grafana.

Grafana loads this datasource automatically on startup.
